package com.baiju.evolve.data;

public class LunchData extends MenuData {
	
	public LunchData(int order, String type, String description, boolean isMandatory, boolean isMultipleAllow) {
		super(order, type, description, isMandatory, isMultipleAllow);
	}
}
